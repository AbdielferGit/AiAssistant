# Secrets y credenciales — NUNCA subir esto a git
.env
*.env
credentials.json
token.json

# Lista de contactos real (PII) — solo se sube la plantilla .example
config/contacts.yaml

# Lista de invitados a la web real (PII) — solo se sube la plantilla .example
config/invited_users.yaml

# Perfil real de un negocio cliente (datos comerciales del cliente) — solo
# se sube la plantilla .example
config/negocio.yaml

# Datos locales (DB, embeddings, sesiones)
data/*
!data/.gitkeep
!data/README.md

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
