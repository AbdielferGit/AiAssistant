anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
google-api-python-client>=2.140.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
google-auth>=2.34.0
# El oauthlib del sistema en algunos hostings (ej. Bluehost/Python 3.9) es
# viejo y no tiene SIGNATURE_RSA — fuerza una versión moderna en el
# virtualenv/user site-packages para que google-auth-oauthlib no choque con él.
oauthlib>=3.2.0
lancedb>=0.13.0
httpx>=0.27.0
pydantic>=2.8.0
pyyaml>=6.0.2
itsdangerous>=2.2.0

# --- Agente "productor_reels" (orchestrator/tools/reel_generator.py) ---
# Genera los .mp4 de las campañas de redes sociales del proyecto de
# prospección. Pesan bastante (moviepy trae numpy/imageio) — si nunca vas
# a usar este agente, puedes omitir este bloque al instalar.
Pillow>=10.4.0
moviepy>=2.0.0
imageio-ffmpeg>=0.5.1
azure-cognitiveservices-speech>=1.40.0

# --- Video real con IA (orchestrator/tools/video_ia.py, agente "productor_reels") ---
# Veo (Google) vía la API oficial de Gemini — google-genai es el SDK
# nuevo, NO confundir con el paquete viejo "google-generativeai".
# beautifulsoup4 extrae texto legible de una URL para fundamentar el
# prompt en contenido real.
google-genai>=0.3.0
beautifulsoup4>=4.12.0
